/** Shared row treatment for WorkspaceSidebar's "Umum" group — icon + label, generous padding, no borders (matches PageTreeItem's tree rows). */
export const sidebarNavRowClass =
  "group/row flex w-full items-center gap-2.5 rounded-sm px-2 py-2.5 text-body-sm text-sidebar-foreground hover:bg-sidebar-accent hover:text-sidebar-accent-foreground";
export const sidebarNavIconClass = "size-4 shrink-0 text-sidebar-foreground/60 group-hover/row:text-sidebar-accent-foreground";
