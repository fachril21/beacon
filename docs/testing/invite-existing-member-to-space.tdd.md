# TDD Evidence Report — Invite Existing Member to Space

**Source plan:** inline `/ecc:plan` output (conversational mode, no `*.prd.md`/`*.plan.md` artifact) — user asked whether the invite-member feature was already functional and, if not, to plan and implement it for real team collaboration. Confirmed with "proceed with this plan," then implemented via `/ecc:tdd-workflow`.

## Key finding that shaped the plan

Space invites already had a full admin UI and a working backend path — but only for one specific case. `useInviteToSpace` wrote a `pending_invites` row, and a `handle_new_user` trigger (`20260806100100_rls_policies.sql`) resolved it — but that trigger only fires on a brand-new `auth.users` INSERT (first-time signup). Inviting an email that **already has a Beacon Account** — the normal case when adding a teammate to a new Space — went nowhere: nothing ever consumed that `pending_invites` row, and `pending_invites` RLS (`pending_invites_admin_only`) restricts SELECT to the inviting Space's admins, so the invited person had no way to even discover the invite existed. This session found and fixed exactly that gap, while confirming the new-signup path already worked as designed.

## User Journeys

1. As a Space admin, when I invite a teammate whose email already has a Beacon Account in my own Organization, they gain access to the Space immediately — no waiting, no dead-end pending row.
2. As a Space admin, when I invite an email with no Account yet, a pending invite is created and resolves automatically the moment that person signs up (existing `handle_new_user` behavior, unchanged).
3. As a Space admin, if I invite an email that belongs to an Account in a *different* Organization, I get a clear rejection instead of a pending invite that could never complete (an Account's `organization_id` is permanent).
4. As a Space admin, I can cancel a stale or mistaken pending invite.
5. As anyone other than a Space admin, I cannot call the invite RPC to grant myself or anyone else access.

## Task Report

### Task 1 — `invite_to_space` RPC + `pending_invites` dedup index
- **Summary:** New migration `20260814000000_invite_to_space_rpc.sql`. `public.invite_to_space(p_space_id, p_email, p_role)` is `SECURITY DEFINER` (required so the existing-Account lookup can see `profiles` across every Organization — `profiles_select_same_organization` RLS would otherwise hide any cross-Org email, which is exactly the case this function must detect), and therefore re-checks the caller's own admin role via `space_role_at_least(p_space_id, 'admin')` before doing anything else, since SECURITY DEFINER bypasses RLS entirely. Branches: existing same-Org profile → upsert into `permissions`, returns `{status: "added", permission}`; existing different-Org profile → `raise exception 'EMAIL_BELONGS_TO_ANOTHER_ORGANIZATION: ...'`; no profile → upsert into `pending_invites` (new unique index `pending_invites_space_id_email_unique` on `(space_id, lower(email))` so re-inviting refreshes the row instead of accumulating duplicates), returns `{status: "invited", invite}`.
- **Verification:** could not run via `supabase test db` (no local Docker Postgres in this environment, same constraint as the pre-existing `rls_policies.test.sql`). Verified instead by applying the migration to the linked remote database and exercising the RPC directly with real fixture data wrapped in a self-rolling-back `do $$ ... raise exception 'INTENTIONAL_ROLLBACK...' $$` block (a `db query --file` call cannot execute an explicit multi-statement `begin; ...; rollback;`, since the CLI prepares the whole file as one statement — a single DO block that always raises at the end achieves the same "verify then discard" effect, since a DO block is one top-level statement and therefore one implicit transaction). This surfaced and fixed a real fixture bug along the way: inserting into `auth.users` fires the *existing* `handle_new_user` trigger, which auto-creates a `profiles` row before the fixture's own explicit `insert` runs — confirming that trigger fires correctly on this database, and requiring the fixture to `on conflict (id) do update` instead of a plain insert.
  - Result once fixture data was reachable: blocked by `permission denied for table users` — the linked `SUPABASE_DB_URL` connection cannot write to `auth.users` directly (Supabase restricts this even for the connection string used by `supabase db push`/`db query`). Real end-to-end verification was completed instead through the actual running application (Task 4 below), which is stronger evidence than synthetic SQL fixtures for this specific behavior.

### Task 2 — pgTAP suite (authored, not executed)
- **Summary:** `supabase/tests/database/invite-to-space-rpc.test.sql`, mirroring the existing `rls_policies.test.sql` conventions exactly (role impersonation via `set local role` / `request.jwt.claim.sub`, `begin`/`rollback`, `plan(6)`). Covers: same-Org existing-Account grant (status + role + zero pending rows), unknown-email pending fallback, cross-Org rejection (`throws_like`), non-admin caller rejection (`throws_like`).
- **Status:** same explicit caveat as the pre-existing suite — authored against the migration's actual schema/logic, not yet run against a live Postgres with `pgtap` installed (confirmed available but not installed on the linked remote — `pg_available_extensions` lists `pgtap 1.3.3` as installable). Should be run via `supabase test db` against a real local stack before trusting as GREEN in CI.

### Task 3 — `useInviteToSpace` / `useCancelInvite` hooks
- **Summary:** `useInviteToSpace` (`src/hooks/use-spaces.ts`) now calls `supabase.rpc("invite_to_space", ...)` instead of a raw `pending_invites` insert, and patches `permissionsStore` or `pendingInvitesStore` depending on the returned `status`. Dropped the now-unused `invitedByUserId` parameter — the RPC reads `auth.uid()` itself (it must, running SECURITY DEFINER). New `useCancelInvite` deletes a `pending_invites` row and removes it from the local store.
- **RED → GREEN:** `npx vitest run src/hooks/use-spaces.test.ts`
  - RED: `PASS (5) FAIL (4)` — `TypeError: supabase.from(...).insert is not a function` (3 tests, old code path) and `TypeError: useCancelInvite is not a function` (1 test, didn't exist).
  - GREEN: `PASS (9) FAIL (0)` after implementation.

### Task 4 — Members page UI (toast differentiation + cancel action)
- **Summary:** `src/app/(workspace)/spaces/[spaceId]/members/page.tsx` — toast now reflects the actual outcome ("Anggota langsung ditambahkan." vs "Undangan terkirim."), a specific error toast for the cross-Org case ("Email ini terdaftar di Organisasi lain."), and a cancel (✕) button on each pending-invite row wired to `useCancelInvite`.
- **Live verification (real app, real data, no synthetic fixtures):** signed in as the existing admin of the "Cakra-Academic" Space (Dibimbing Organization, which has exactly two real member Accounts). Found a genuine pre-existing stale `pending_invites` row for `fachril21@gmail.com` — an email that already had a real same-Org Account, created before this fix, sitting permanently unresolved — the exact bug this session set out to fix, present in production data.
  1. Cancelled the stale invite via the new ✕ button → row disappeared, toast "Undangan dibatalkan."
  2. Re-invited `fachril21@gmail.com` (existing same-Org Account) as Editor → toast **"Anggota langsung ditambahkan."**, and the account appeared in the member list immediately with the assigned role. No `pending_invites` row was created (the "added" branch returns before touching that table).
  3. Invited a brand-new unknown email (`brand-new-teammate@dibimbing.id`) → toast **"Undangan terkirim."**, appeared under "Undangan tertunda" with a "Menunggu" badge.
  4. Cancelled that test invite via the ✕ button to leave no test data behind.
- **Known gap (flagged, not silently cut):** the cross-Organization rejection and non-admin-caller rejection paths were **not** exercised live — the linked database currently has only one Organization with any real member Accounts (Cakrawala University has zero), so there was no second-Organization Account available to invite without fabricating one, and fabricating `auth.users` rows is blocked by the `permission denied for table users` restriction found in Task 1. These two paths are verified by direct code review of the migration's straightforward `plpgsql` guard clauses (`if v_profile_org_id <> v_space_org_id then raise exception ...` / `if not space_role_at_least(...) then raise exception ...`) and by the pgTAP suite (Task 2), which is authored but not executed for the reasons stated there.

## Test Specification

| # | What is guaranteed | Test file / evidence | Type | Result |
|---|---|---|---|---|
| 1 | Inviting an existing same-Organization Account calls `invite_to_space` and grants the Permission immediately, patching `permissionsStore` and leaving `pendingInvitesStore` untouched | `src/hooks/use-spaces.test.ts` | unit | PASS |
| 2 | Inviting an unknown email creates/patches a `pending_invites` row via the RPC's `invited` status | `src/hooks/use-spaces.test.ts` | unit | PASS |
| 3 | A cross-Organization rejection from the RPC is rethrown and touches neither local store | `src/hooks/use-spaces.test.ts` | unit | PASS |
| 4 | `useCancelInvite` deletes the row server-side and removes it from `pendingInvitesStore` | `src/hooks/use-spaces.test.ts` | unit | PASS |
| 5 | An existing same-Organization Account is granted the Permission immediately, with zero `pending_invites` rows created (RPC logic) | `supabase/tests/database/invite-to-space-rpc.test.sql` | pgTAP (authored, not executed — see Task 2) | not run |
| 6 | An unknown email still falls back to `pending_invites` (RPC logic) | `supabase/tests/database/invite-to-space-rpc.test.sql` | pgTAP (authored, not executed) | not run |
| 7 | A different-Organization email is rejected with `EMAIL_BELONGS_TO_ANOTHER_ORGANIZATION` (RPC logic) | `supabase/tests/database/invite-to-space-rpc.test.sql` | pgTAP (authored, not executed) | not run |
| 8 | A non-admin Space member cannot call `invite_to_space` (RPC logic) | `supabase/tests/database/invite-to-space-rpc.test.sql` | pgTAP (authored, not executed) | not run |
| 9 | End-to-end: an existing-Account invite grants access immediately in the real running app | Live browser verification, this session (Task 4) | manual/E2E | PASS |
| 10 | End-to-end: an unknown-email invite creates a visible pending row in the real running app | Live browser verification, this session (Task 4) | manual/E2E | PASS |
| 11 | End-to-end: the cancel-invite action removes a pending row in the real running app | Live browser verification, this session (Task 4) | manual/E2E | PASS |

## Addendum — Invite Email Delivery

**Follow-up scope, same session.** After the above landed, the user asked directly: "The invitation email hasn't been sent to the invited email address. Does the user need to already be registered on the platform first?" Answer: no, but nothing notified the invited person either way — `useInviteToSpace` only ever wrote database rows; no email-sending code existed anywhere in the app. User chose "Supabase's built-in SMTP" (the same delivery path as signup-confirmation emails) over a separate provider (Resend) or skipping it.

### Task 5 — Real invite email via `auth.admin.inviteUserByEmail`

- **Summary:** new `POST /api/spaces/[spaceId]/invite` Route Handler (`src/app/api/spaces/[spaceId]/invite/route.ts`). Runs the `invite_to_space` RPC under the caller's own cookie session first (unchanged authorization), then — only for the "no Account yet" branch — calls `auth.admin.inviteUserByEmail` with the service-role client, since sending a real email needs privilege the browser's anon key can never have. `useInviteToSpace` now posts to this route instead of calling the RPC directly from the client.
- **A real timing subtlety, found and handled:** `inviteUserByEmail` creates the `auth.users` row *immediately*, before the invited person does anything — which fires the pre-existing `handle_new_user` trigger and can resolve the `pending_invites` row this same request just created into a real `permissions` row before the request even returns. The route re-queries the actual resulting state (profile + permission, via the admin client) instead of trusting the pre-email RPC snapshot, so the client never receives a stale "invited" status for someone who was, functionally, already granted access. `emailSent` is reported separately so a delivery failure (SMTP misconfigured, provider error) is surfaced as a distinct warning rather than a false hard error for something that succeeded at the data layer.
- **RED → GREEN:** `npx vitest run src/hooks/use-spaces.test.ts`
  - RED: `PASS (6) FAIL (4)` — `TypeError: supabase.rpc is not a function` (the tests now expect a `fetch` call to the new route; removing the `rpc` mock surfaced the old direct-RPC code path).
  - GREEN: `PASS (10) FAIL (0)` after switching `useInviteToSpace` to `fetch("/api/spaces/{id}/invite", ...)`.
- **Live verification (real app, real SMTP, real data) — the strongest evidence in this report:** invited a genuinely unregistered test address through the actual running UI. Captured the real route response by patching `window.fetch` in the page (not a mock): `{"status":"added","permission":{...},"emailSent":true}`. This confirms, against the live project, all of: the RPC created the pending invite; `inviteUserByEmail` sent a real email through the project's configured SMTP (`emailSent: true` — no error from the Auth API); the existing `handle_new_user` trigger fired on the new `auth.users` row and resolved the pending invite into a real permission *within the same request*; and the route's re-query correctly reported the final "added" state instead of the stale "invited" one. Confirmed the new member appeared in the Members UI after a reload (a pre-existing, out-of-scope `useUsers` cache-staleness reason it didn't appear instantly in the same session — the permission was real immediately; only the joined profile list needed a refetch).
- **Cleanup:** deleted the test account via `auth.admin.deleteUser` (a one-off Node script using the service-role key, since this action needs the Admin API — direct SQL can't safely remove an `auth.users` row). Verified zero residual rows afterward.
- **A genuine autofill artifact, documented rather than hidden:** during manual testing, a coordinate-based UI click landed on a stale browser-autofill suggestion instead of the freshly-typed test email, producing an unintended real invite+email send to a different address (`fachril@dibimbing.id`). Not a defect in the shipped code — a browser-automation flake during verification, corrected by re-driving the click through the actual DOM element instead of screen coordinates.
  - **Correction, found in the next session:** the note above originally claimed this invite "did not resolve into a permission." That was wrong — it verified zero rows *at the time*, but `inviteUserByEmail`'s trigger-driven resolution can land slightly after the route's own re-query returns (a read-after-write race — see Known Gaps below), so the route reported `"invited"` while the permission was actually created moments later, invisible until a reload. The stray permission (and its underlying orphaned `auth.users` row) were found and cleaned up in the invite-redirect/forgot-password/remove-member session via the new remove-member feature plus `auth.admin.deleteUser`. Recorded here because reporting a verification claim that later turned out to be incomplete is worth surfacing, not quietly editing away.

## Coverage and Known Gaps

- `src/hooks/use-spaces.test.ts`: **10/10 passing** (`npx vitest run src/hooks/use-spaces.test.ts`), up from 5 at the start of this task.
- Full suite: **248/248 passing** (`npx vitest run`) — the previously-flaky `stepper-block.test.tsx` failure (noted below) did not reproduce on this run, consistent with it being a test-isolation flake rather than a real regression: the same file also passes 12/12 in isolation, and no commit in this session touches it (last touched in `614c6df`, an unrelated prior commit).
- `npx tsc --noEmit --pretty false` → clean.
- `npx eslint` on every touched file → clean.
- **Known gaps**, stated explicitly rather than swept under a generic "done":
  1. The pgTAP suite (`invite-to-space-rpc.test.sql`) is authored but not executed — this environment has no local Docker Supabase stack, matching the exact same caveat the pre-existing `rls_policies.test.sql` already carries. Run via `supabase test db` before trusting as CI-verified.
  2. The cross-Organization rejection and non-admin-caller rejection RPC branches were verified by code review + the (unexecuted) pgTAP suite, not by a live run against real data — the linked database doesn't currently have a second Organization with any member Accounts to test against without fabricating `auth.users` rows, which this session's DB connection is not permitted to do directly.
  3. No self-serve way exists yet for an *already-signed-in* User to see "you were just added to a new Space" beyond it appearing in their sidebar on next load — no toast/notification fires for the invited person in this pass, matching the PRD's existing scope (Epic 19's Slack/notification integrations remain deferred).
  4. The existing-same-org "added" branch still sends no email at all (by design — Supabase Auth has no generic transactional-email template for "you were granted access," only auth-flow-specific ones). That person finds out only by seeing the Space appear in their own sidebar next time they load the app.
  5. `useUsers` cache staleness (pre-existing, not introduced here): a newly-added member's row doesn't render in the Members list within the same client session until the page is reloaded, even though the underlying permission is granted immediately — `allUsers` isn't invalidated when a new profile is created by this flow.
  6. **Found this session, not yet fixed:** `inviteUserByEmail`'s trigger-driven resolution (`handle_new_user`) can commit slightly after `/api/spaces/[spaceId]/invite`'s own re-query reads the database, so the route can report `status: "invited"` for an invite that resolves into a real `permissions` row moments later — invisible in the UI until a reload. Confirmed via a real (accidental) repro this session: an invite reported as pending had, after reload, already become a real Editor membership. Low severity (no data loss — the grant is real either way, and cancelling an already-resolved `pending_invites` row is a safe no-op since the row is already gone) but worth a follow-up: either poll/retry the re-query with a short backoff, or accept the UI staleness and document it as expected.

## Addendum 2 — Invite Redirect, Forgot Password, Remove Member

**Follow-up scope, next session.** User asked for three more things in one pass: (1) the invite email's link should send a not-yet-registered person to complete sign-up rather than a sign-in page that needs a password they don't have; (2) a forgot-password mechanism; (3) a remove-member action in the Space Members UI.

### Task 6 — Shared "set your password from an email link" infrastructure

- **Summary:** `useSession` gains `requestPasswordReset` (`auth.resetPasswordForEmail`, redirects to `/reset-password`) and `updatePassword` (`auth.updateUser({password})`) — one shared primitive, since completing an invite and resetting a forgotten password are mechanically identical (an email link establishes a session; the User then just needs to set a password). New shared `SetPasswordForm` component (`src/components/auth/set-password-form.tsx`, `mode: "invite" | "recovery"`) handles all three states every such screen needs: loading (session still resolving), invalid/expired link (no session after loading), and the actual password+confirm form.
- **RED → GREEN:** `npx vitest run src/hooks/use-session.test.tsx src/hooks/use-spaces.test.ts src/components/auth/set-password-form.test.tsx`
  - RED: `PASS (16) FAIL (6)` — new `useSession` methods didn't exist (`TypeError: ... is not a function`), `useRemoveMember` didn't exist, `set-password-form.tsx` module not found.
  - GREEN: `PASS (27) FAIL (0)`.

### Task 7 — Wire up the three pages/route change

- **Summary:** the Space-invite route (`src/app/api/spaces/[spaceId]/invite/route.ts`) now redirects to `/complete-invite` instead of `/sign-in`. New `/complete-invite` and `/reset-password` pages render `SetPasswordForm`. New `/forgot-password` page requests a reset email (always shows the same "check your email" confirmation regardless of outcome — Supabase's own anti-enumeration behavior already covers this, no extra hiding logic needed). Sign-in page links to `/forgot-password`.
- **A real bug found by live verification, not by the test suite:** the `(auth)` route group's layout unconditionally redirected any authenticated User to Workspace Home. `complete-invite` and `reset-password` *require* an active session — that's the whole mechanism, established by the email link's URL token — so left as-is, every real invited or password-resetting User would have been bounced away before ever seeing the password form, silently breaking both flows. `SetPasswordForm`'s own unit tests mock `useSession` directly and never exercise the real layout, so they couldn't have caught this. Fixed by excluding those two paths from the redirect (`src/app/(auth)/layout.tsx`).
- **Live verification:** navigated to `/complete-invite` while authenticated (simulating the session state a real invited User would have right after clicking their email link) — confirmed the invite-mode form renders correctly ("Selesaikan pendaftaran Anda" / password + confirm fields) instead of being redirected away, both before locating the bug (bounced to `/`) and after the fix (form rendered). Did not submit the form, since this session's authenticated account is a real Account, not a throwaway. Verified `/forgot-password` still correctly redirects an already-authenticated User away (no reason for a signed-in person to see it).

### Task 8 — Remove member

- **Summary:** `useRemoveMember` (`src/hooks/use-spaces.ts`) deletes a `permissions` row (RLS: `permissions_delete_admin_only`, already existed — no migration needed). Members page gets a "Hapus" action per row, reusing the existing `DeleteConfirmDialog` component (the same destructive-confirm pattern used for deleting a Space/Page) rather than the lighter bare-✕ pattern used for cancel-invite, since removing a member revokes real, already-effective access — a meaningfully different stakes level than cancelling a not-yet-effective invite.
- **RED → GREEN:** covered in Task 6's run above (`useRemoveMember` tests included in `use-spaces.test.ts`'s RED→GREEN).
- **Live verification — and real cleanup value:** the Members page happened to be carrying a genuine leftover artifact from the previous session's invite-email testing (`fachril@dibimbing.id`, a real Editor permission that had silently resolved via the race documented in Known Gap #6 above, invisible until this session's reload). Used the brand-new remove-member button to delete it through the actual UI — confirming the feature works end-to-end and, as a side effect, actually finding and fixing the previous session's incomplete cleanup. Underlying orphaned `auth.users` row removed via `auth.admin.deleteUser` (same one-off-script pattern as prior cleanups, since direct SQL can't safely remove an `auth.users` row).

## Coverage and Known Gaps (Addendum 2)

- `src/hooks/use-session.test.tsx`, `src/hooks/use-spaces.test.ts`, `src/components/auth/set-password-form.test.tsx`: all passing (27 across the three, per Task 6).
- Full suite: **259/259 passing** (`npx vitest run`).
- `npx tsc --noEmit --pretty false` → clean. `npx eslint` on every touched file → clean.
- **Known gaps:**
  1. The invite-completion and password-reset forms were verified structurally (rendering, validation, the auth-layout redirect fix) but not by actually clicking a real email link end-to-end — that requires inbox access this environment doesn't have. The underlying mechanism (Supabase's `detectSessionInUrl` + the existing `SessionProvider`) is standard and already proven working for the existing sign-in/sign-up flows; the new pages just consume the same session state.
  2. No "last admin" or self-removal protection on remove-member — an admin can remove themselves from a Space, same as they already could by demoting their own role via the existing role dropdown. Not a new risk introduced here, but also not specifically guarded against.
  3. The remove-confirmation dialog's title falls back to an empty name when the target profile's `name` field is blank (happens for accounts auto-created via `inviteUserByEmail`, which sets no name) — cosmetic only, the email is still shown in the row itself.

## Addendum 3 — Invite Email Must Never Be Blocked by Account Existence

**Follow-up scope, next session.** User tested the shipped flow and reported: "the mechanism where if the invited user doesn't have a Beacon account, the email can't be sent — that's wrong." Root cause, found by re-reading this doc's own Addendum 1/2 known-gap notes: this codebase's own invite-testing history repeatedly produced orphaned `auth.users` rows (an invite attempt creates the `auth.users` row immediately, before the person ever completes it — see Addendum 1 Task 5 and the "genuine autofill artifact" note). Re-inviting such an address — someone with **no `profiles` row, i.e. no Beacon Account by product definition** — hit `inviteUserByEmail`'s hard failure for an already-existing `auth.users` row, and the route reported `emailSent: false` with no retry. From the user's perspective: exactly "no Account yet, and the email can't be sent."

### Task 9 — Fallback to `resetPasswordForEmail` on an already-registered auth error

- **Summary:** `src/app/api/spaces/[spaceId]/invite/route.ts` — when `inviteUserByEmail` fails with an "already registered" error, the route now falls back to `admin.auth.resetPasswordForEmail(email, { redirectTo: "/complete-invite" })`, which sends real mail via the same SMTP path to any existing `auth.users` row regardless of confirmation state. `emailSent` reflects whichever call actually succeeded. The brand-new-email path (no `auth.users` row at all) is unchanged — `inviteUserByEmail` still handles that directly and this fallback never triggers.
- **RED → GREEN:** `npx vitest run "src/app/api/spaces/[spaceId]/invite/route.test.ts"` (new file — no route-level test existed before this session, only hook-level tests against a mocked `fetch`).
  - RED: `PASS (2) FAIL (1)` — the already-registered scenario expected `resetPasswordForEmail` to be called; it never was.
  - GREEN: `PASS (3) FAIL (0)` after adding the fallback and broadening the error-phrase match (GoTrue's actual message is "already **been** registered", not the contiguous "already registered" this codebase's `signUp` handler already matched on — the new `isAlreadyRegisteredAuthError` in `route.ts` checks for "already" + "registered" independently, plus "already exists").
- **Confirms unchanged:** landing on `/complete-invite` with a valid session already renders `SetPasswordForm(mode="invite")` — "Selesaikan pendaftaran Anda" / "Buat akun" — which was already the "prompted to create an account" step the user asked for. No change needed there; the actual gap was purely the email-delivery fallback.

## Coverage and Known Gaps (Addendum 3)

- New: `src/app/api/spaces/[spaceId]/invite/route.test.ts`, 3/3 passing.
- Full suite: **262/262 passing** (`npx vitest run`).
- `npx tsc --noEmit --pretty false` → clean. `npx eslint` on the two touched files → clean.
- **Known gap:** the fallback email uses Supabase's *Recovery* template copy, not an *Invite*-flavored one — Supabase Auth has no "resend invite" template distinct from the original invite email. Acceptable tradeoff (a real email that gets them to the same "create your account" screen) but the subject/body will read as a password reset rather than an invite for this one fallback path. Not verified against a real inbox this session — reasoning and the RED→GREEN route test are the evidence; live SMTP verification would require deliberately orphaning a real `auth.users` row again, which this session avoided given the cleanup cost documented in Addendum 1/2.

## Addendum 4 — Root Cause Was Supabase's Rate-Limited Default Mailer; Switched to Resend SMTP

**Follow-up scope, next session.** User reported invite emails were still not being delivered even after Addendum 3's fallback. Live diagnostic (a disposable-address `inviteUserByEmail` call via the service-role client, cleaned up immediately) against the real project surfaced the actual cause — not a code bug at all:

```json
{ "name": "AuthApiError", "message": "email rate limit exceeded", "code": "over_email_send_rate_limit", "status": 429 }
```

`supabase/config.toml`'s `[auth.rate_limit] email_sent` was `2` — Supabase's built-in testing-only mailer default. All the invite-testing across this feature's build history (Addenda 1-3: multiple real test invites, cleanup scripts, orphaned accounts) had been burning through that same 2-per-hour quota. No application code could fix this — it's enforced by Supabase Auth itself before the route handler's own logic ever runs. Planned via `/ecc:plan` and confirmed with the user: switch to Resend as a custom SMTP provider, starting with a sandbox sender since no domain was verified in Resend yet.

### Task 10 — Wire Resend into `supabase/config.toml`

- **Summary:** `[auth.email.smtp]` now points at Resend (`smtp.resend.com`, port `587`, user `resend`, `pass = "env(RESEND_API_KEY)"`, `admin_email = "onboarding@resend.dev"`, `sender_name = "Beacon"`). Only the secret `pass` field uses `env()` substitution — the non-secret fields (`host`/`port`/`user`) are hardcoded because `env()` substitution specifically for the `host` field is a known, currently-open Supabase CLI bug ([cli#3255](https://github.com/supabase/cli/issues/3255)), found via research before implementing rather than discovered the hard way. `[auth.rate_limit] email_sent` raised from `2` to `30` — that cap is enforced by Supabase Auth itself independent of which SMTP relay sits behind it, so leaving it at `2` would have kept throttling Resend-backed sends too.
- **No application code changed.** `route.ts` and `use-session.tsx` already call Supabase Auth directly (`inviteUserByEmail`, `resetPasswordForEmail`, `signUp`) — none of them are aware of which mailer is configured. This is the key scope finding from the plan: switching providers is a single project-level config change, not N per-email-type code changes.
- **Validate:** `npx vitest run` → 262/262 passing (unchanged, config-only diff). `npx tsc --noEmit --pretty false` → clean. No test framework applies to a TOML config file; validation here was inspection plus the live diagnostics below.
- **Blocked path, documented rather than forced:** intended to apply this via `supabase config push`, but the CLI session in this environment is authenticated against a different Supabase account than the one owning Beacon's project — confirmed via a read-only check, `supabase projects api-keys --project-ref jrdtpmtpjowouknmuxid` → `403`. User applied the equivalent values by hand instead, via Dashboard → Authentication → Emails → SMTP Settings.

### Task 11 — Live verification against the real project (multiple rounds, root-caused a second issue along the way)

All rounds used a disposable diagnostic script (`.diag-invite.mjs`, gitignored via `.env*`-adjacent scratch pattern, deleted after each round — never committed) calling the service-role client directly, mirroring the same technique validated in Addendum 1.

1. `inviteUserByEmail` → a fake `@example.com` address: `AuthRetryableFetchError: Error sending invite email (500)`. Ambiguous — GoTrue doesn't surface the underlying SMTP provider's rejection reason.
2. `resetPasswordForEmail` → the user's own `fachril21@gmail.com`: same generic `500`. Still ambiguous — this ruled out "fake recipient domain" as the cause but not "sandbox-sender restriction," since `fachril21@gmail.com` turned out not to be the email the user's Resend account is registered under.
3. **User checked Resend's own dashboard logs** (this session couldn't — no Resend API access) and found the actual rejection: *"The resend.dev domain is for testing and can only send to your own email address. To send to other recipients, verify a domain and update the from address to use it."* This is expected, documented Resend sandbox behavior, not a config defect — confirms `[auth.email.smtp]` is correctly wired and Supabase is correctly relaying to Resend; Resend itself is the one rejecting on sandbox-recipient grounds.
4. `inviteUserByEmail` → `fachril@cakrawala.ac.id` (the Resend account's own registered address): `AuthApiError: email_exists` — expected, that address already has a real Beacon account from earlier sessions, so `inviteUserByEmail` short-circuits before ever attempting SMTP.
5. `resetPasswordForEmail` → `fachril@cakrawala.ac.id` (same address, but via the fallback path since the account already exists): **`error: null`.** User confirmed the email actually arrived in that inbox — real, end-to-end proof that Supabase Auth → Resend → real inbox delivery now works, for the same code path `forgot-password` and the Addendum 3 fallback both already use.

### Known gap — recovery link showed "expired" on click

The user reported the link in the round-5 email showed as expired when clicked. Not root-caused this session; three candidate causes were surfaced but not individually isolated:
1. `auth.email.otp_expiry` is `3600`s (1 hour) — the diagnostic rounds above spanned enough real conversation time that the token plausibly outlived its window before being clicked.
2. Institutional (`.ac.id`) inboxes commonly run a mail-security gateway (Microsoft Safe Links, Google Workspace link scanning) that auto-visits every link in an email before the human ever clicks it — since recovery links are single-use, this silently burns the token first. Very common with magic-link/recovery email generally, unrelated to Resend.
3. The diagnostic script's `redirectTo: "http://localhost:3000/reset-password"` doesn't exactly match `config.toml`'s `additional_redirect_urls` (`https://127.0.0.1:3000`, different host and scheme) — a mismatch here can also produce a Supabase error page, though typically with different wording than "expired."
- **This is explicitly unresolved**, separate from this session's actual scope (email not sending, now proven fixed). Recorded here rather than silently dropped so it isn't mistaken for closed.

## Coverage and Known Gaps (Addendum 4)

- No new automated tests — this addendum's scope was infrastructure config (`supabase/config.toml`) plus live verification, not new application logic. `npx vitest run` re-run as a regression check only: 262/262 passing, unchanged from Addendum 3.
- `npx tsc --noEmit --pretty false` → clean.
- **Known gaps**, stated explicitly:
  1. Sandbox sender (`onboarding@resend.dev`) still only delivers to the Resend account's own registered address (`fachril@cakrawala.ac.id`) — real invited/recovering users at other addresses will not receive mail until a domain is verified in Resend and `admin_email` in `supabase/config.toml` (plus the Dashboard SMTP sender) is updated to an address on that domain.
  2. The "link expired" symptom from Task 11 round 5 is unresolved (see above) — the three candidate causes are ranked by plausibility but not individually confirmed.
  3. `supabase config push` remains blocked for this CLI session (wrong authenticated account) — `supabase/config.toml` is the versioned source of truth, but the live project's actual SMTP settings were applied by hand via the Dashboard, not synced from the repo. If the two ever drift, the Dashboard is currently the one actually in effect.

## Addendum 5 — Organization Invite Had the Same Root Cause; Reverted from Resend to Supabase's Built-In Mailer (Self-Hosted, No Rate Limit)

**Follow-up scope, later session.** User reported an Organization invite (`/api/organizations/[id]/invite/route.ts`, which mirrors the Space invite route's `inviteUserByEmail` call exactly) also never delivered, and asked whether this was caused by an unrelated same-day Postgres schema migration (`public` → `beacon`). Investigated via `git log`/`git show` rather than a live diagnostic this time — no live send was needed since the cause was already on record.

- **Timeline check ruled out the schema migration:** Addendum 4 (Resend sandbox-sender limitation) was committed 2026-08-14 — over a month before the `beacon` schema migration (2026-09-22). `supabase/config.toml`'s `[auth.email.smtp]` block was untouched by that migration (confirmed via `git log -- supabase/config.toml`) and still pointed at `admin_email = "onboarding@resend.dev"` right up to this session. `inviteUserByEmail`/`resetPasswordForEmail` are Supabase Auth Admin API calls against `auth.users`, independent of which Postgres schema the app's own tables live in (`getSupabaseSchema()`), so a schema rename could not have been the cause here regardless.
- **Conclusion:** same root cause as Addendum 4 — the Resend sandbox sender only delivers to the Resend account's own verified address, so any Organization invite to a real teammate's address was silently rejected by Resend, not by Beacon's code or by the migration.
- **User's decision, with a trade-off flagged first:** rather than verifying a domain in Resend, the user chose to revert to Supabase Auth's built-in mailer. The original reason the team left the built-in mailer (Addendum 4: `AuthApiError: email rate limit exceeded`, `over_email_send_rate_limit`, ~2 emails/hour) was raised as a risk before proceeding — user confirmed it does not apply: this project's Supabase instance is **self-hosted on the company's own VPS** (`PROJECT.md` §9.2), not Supabase Cloud, so that ceiling isn't in effect here.

### Task 12 — Disable custom SMTP in `supabase/config.toml`

- **Summary:** `[auth.email.smtp] enabled` flipped from `true` to `false`; the Resend-specific fields (`host`, `port`, `user`, `pass`, `admin_email`, `sender_name`) removed. `[auth.rate_limit] email_sent` left at `30` (harmless either way; only matters if custom SMTP is re-enabled later). No application code changed — same finding as Addendum 4 Task 10: which mailer is configured is a project-level setting the route handlers (`inviteUserByEmail`, `resetPasswordForEmail`, `signUp`) are never aware of.
- **Validate:** `npx vitest run` → 393/393 passing (unchanged, config-only diff — includes both invite routes' existing mailer-agnostic tests, which mock the Supabase Admin client and never assert on which SMTP relay is behind it).
- **Known gap, stated explicitly:** this repo's `supabase/config.toml` documents intended settings only. Because this Supabase instance is self-hosted on the company's own VPS rather than Supabase Cloud, the live instance's actual GoTrue SMTP configuration is whatever `GOTRUE_SMTP_*` environment variables are set in that VPS's own deployment (docker-compose/env file) — outside this repo, and outside what Claude Code can reach or verify. Whoever administers the VPS must apply the equivalent change (disable/unset the GoTrue SMTP env vars, or otherwise point back to the built-in mailer) for this to take effect on the live project. Manual end-to-end verification (a real invite to a real teammate address) is the user's own next step, as originally requested.

## Coverage and Known Gaps (Addendum 5)

- No new automated tests — same as Addendum 4, this is an infrastructure config change with no testable application-code delta. `npx vitest run` → 393/393 passing.
- **Known gaps**, stated explicitly:
  1. The live VPS's actual SMTP env vars have not been changed by this session — only this repo's `supabase/config.toml` documentation of intended settings. See Task 12 above.
  2. Not verified against a real inbox this session — the user stated they will test manually.

## Addendum 6 — Organization Invite Now Always Requires Confirmation (Except Existing-Member Role Changes)

**Follow-up scope, later session.** While manually testing the Addendum 5 fix, the user invited an email that already had a Beacon Account and got "Anggota langsung ditambahkan." (instant add) with no `organization_invitations` row — expected given the code at the time (any existing Account short-circuited straight to `organization_memberships`), but not what the user wants: they asked for the invited person to always confirm via email before joining, not be added silently.

**Design decision, confirmed with the user first:** the same "added" fast path was also the only existing way to change an already-a-member's role (re-invite with a new role → `on conflict ... do update set role`). Removing it entirely would have silently broken that. User confirmed: keep instant role-update for someone **already a member of this Organization** (nothing to confirm — they're already inside); require confirmation for everyone else, whether or not they have a Beacon Account elsewhere.

### Task 13 — `beacon.invite_to_organization`: only an existing member's role changes instantly

- **Summary:** new migration `20260923000000_organization_invite_requires_confirmation.sql`, `create or replace function beacon.invite_to_organization`. The existing-Account lookup (`beacon.profiles` by email) now also checks whether that profile already has an `organization_memberships` row for **this** Organization. Only then does it update the role in place and return `status: "added"`. Every other case (no profile at all, or a profile that exists but isn't a member of this Organization yet) falls through to the unchanged pending-`organization_invitations` insert/upsert and returns `status: "invited"` — identical code path, and therefore identical email-sending behavior in the route handler, to the always-was-true brand-new-email case.
- **No changes needed in `route.ts` or `useInviteToOrganization`** — both already branch generically on `status: "added" | "invited"`; the route's existing `inviteUserByEmail` → `resetPasswordForEmail` fallback (for an email that already has an `auth.users` row) already produces a real, working confirmation email for this newly-expanded "invited" case without any route code change.
- **A real bug found and fixed along the way:** that fallback's email `redirectTo` already pointed at `/accept-invite?token=...`, but `src/app/(auth)/layout.tsx`'s `SKIP_AUTHENTICATED_REDIRECT` only exempted `/complete-invite` from the "authenticated → bounce to Workspace Home" redirect. An existing Account's confirmation email establishes a session on arrival (same as the recovery-link mechanism `complete-invite` already relies on) — without the exemption, `AuthLayout`'s effect would have redirected the user to `/` before `AcceptInviteContent`'s own effect could call `accept_organization_invite`, silently dropping the accept. Added `/accept-invite` to the exemption set.
- **RED → GREEN (`src/app/(auth)/layout.test.tsx`, new file):** RED confirmed by stashing the `layout.tsx` fix and re-running — `PASS (2) FAIL (1)`, the new accept-invite case failing with `router.replace` called with `"/"` when it shouldn't have been. Restored the fix: `PASS (3) FAIL (0)`.
- **`supabase/tests/organization-invite-rpcs.sql` rewritten** (RED not executable this session — see Known Gaps): Case 1 now expects `status: "invited"` and zero `organization_memberships` rows for an existing-but-non-member Account (B), instead of the old instant-add expectation. New Case 1b: re-inviting an *already-a-member* Account (C, a second Org One member added to the fixtures specifically so Case 4/6/7's existing dependency on D staying a plain `member` isn't disturbed) still gets `status: "added"` with the role updated in place and no invitation row. New Case 1c: B accepts their own pending invite via `accept_organization_invite` and becomes a member — the actual end-to-end confirm-via-email journey this change is for.
- **Validate:** `npx vitest run` → 396/396 passing (393 prior + 3 new `layout.test.tsx` cases). `npx tsc --noEmit --pretty false` → clean. `npx eslint` on both touched TypeScript files → clean.

## Coverage and Known Gaps (Addendum 6)

- New: `src/app/(auth)/layout.test.tsx`, 3/3 passing, including a real RED→GREEN cycle (verified by stashing the fix and re-running before restoring it).
- **Known gap, stated explicitly:** `supabase/tests/organization-invite-rpcs.sql` was rewritten to match the new expected behavior but **not executed** this session — this environment's local Docker daemon isn't running (`docker ps` fails to reach the daemon), and running it against the live self-hosted instance instead (even wrapped in `begin; ... rollback;`, as the file already is) is a live-database action this session didn't have explicit permission for. The user should run it themselves via `docker exec -i supabase_db_beacon psql -U postgres < supabase/tests/organization-invite-rpcs.sql` against a local stack, or the equivalent against the VPS, before treating the SQL migration as proven rather than just reviewed.
- Manual end-to-end verification (inviting a genuinely non-member email, confirming the email arrives and clicking through to actually join) is the user's own next step, same as the rest of this feature.
